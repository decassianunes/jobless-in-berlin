.DS_Store
*.swp
.vscode/
.idea/

# Claude local settings (preview config is OK to commit, local settings aren't)
.claude/settings.local.json

# Per-conversation session logs (kept local, never pushed)
conversation_logs/

# Ideas / planning notes (kept local, never pushed)
IDEAS.md

# Throwaway local card-design preview (no API, never pushed)
card-preview.html
